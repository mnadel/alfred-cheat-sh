#!/usr/bin/env python3

import json

data = {
    "items": []
}

with open("cheatsheets.txt", mode="r", encoding="utf8") as f:
    for line in f:
        kw = line.strip()
        data["items"].append({
            "title": kw,
            "arg": kw,
        })

print(json.dumps(data, indent=4))
